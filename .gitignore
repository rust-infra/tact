/target/
# Update signing keys: the private half must never be committed (`cargo
# packager signer generate` writes these).
*.key
*.key.pub
*.signing-private-key
# Packaged installers (`cargo packager` output), not source.
/dist/
/videos/
# Local runtime state (state_store / stream_store), not source.
/data/
*.log
.env
.DS_Store
.ideal
.idea/
.claude
.deepseek
.openai
.superpowers/
.tact/
skills
.qoder/
.workbuddy-ai/
.worktrees/
.tmp/
tmp/
crates/tui/agent_log_*.txt
*.jpeg
